# Example environment variables for production deploy (rename to .env and secure values)

DOMAIN_NAME=your-domain.example.com
POSTGRES_PASSWORD=change_this_secure_password
GRAFANA_ADMIN_PASSWORD=change_this_secure_password
X_API_KEY=OpsPulse_Super_Secret_2026

# Optional: override API/Grafana ports if needed
API_PORT=5000
GRAFANA_PORT=3000
